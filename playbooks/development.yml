- hosts: localhost
  become: true
  tasks:
    - name: Update apt package list
      apt:
        update_cache: yes
    - name: Install Git
      apt:
        name: git
    - name: Install GitKraken
      snap:
        name: gitkraken
