- name: Install utilities apps
  hosts: localhost
  become: true
  tasks:
    - name: Update apt package list
      apt:
        update_cache: yes
    - name: Install KeePassXC
      snap:
        name: keepassxc
    - name: Install Midnight Commander
      apt:
        name: mc
